#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a = 125;
    float f =3.5;
    
    char ch[5] ={'d'};
   cout << typeid(a).name() << endl;
   cout << typeid(f).name() << endl;
   cout << typeid(ch).name() << endl;
   // a = string(a);
   // cout << typeof(a)<<endl;
    return 0;
}